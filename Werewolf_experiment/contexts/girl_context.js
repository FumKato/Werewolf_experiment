if(Meteor.isClient){
	var operations = {
	
	};

	girl_context = new Context('girl', operations);
}

if(Meteor.isServer){
	var operations = {
	
	};

	girl_context = new Context('girl', operations);
};
