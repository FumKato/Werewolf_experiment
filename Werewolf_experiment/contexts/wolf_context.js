if(Meteor.isClient){
	var operations = {
	
	};

	wolf_context = new Context('wolf', operations);
}

if(Meteor.isServer){
	var operations = {
	
	};

	wolf_context = new Context('wolf', operations);
};
