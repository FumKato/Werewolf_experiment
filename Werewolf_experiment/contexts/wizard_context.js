if(Meteor.isClient){
	var operations = {
	
	};

	wizard_context = new Context('wizard', operations);
}

if(Meteor.isServer){
	var operations = {
	
	};

	wizard_context = new Context('wizard', operations);
};
