if(Meteor.isClient){
	var operations = {
	
	};

	hunter_context = new Context('hunter', operations);
}

if(Meteor.isServer){
	var operations = {
	
	};

	hunter_context = new Context('hunter', operations);
};
