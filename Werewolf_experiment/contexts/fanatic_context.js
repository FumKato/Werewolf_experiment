if(Meteor.isClient){
	var operations = {
	
	};

	fanatic_context = new Context('fanatic', operations);
}

if(Meteor.isServer){
	var operations = {
	
	};

	fanatic_context = new Context('fanatic', operations);
};
