if(Meteor.isClient){
	var operations = {
	
	};

	villager_context = new Context('villager', operations);
}

if(Meteor.isServer){
	var operations = {
	
	};

	villager_context = new Context('villager', operations);
};
