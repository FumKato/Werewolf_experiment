if(Meteor.isClient){
	var operations = {
	
	};

	mason_context = new Context('mason', operations);
}

if(Meteor.isServer){
	var operations = {
	
	};

	mason_context = new Context('mason', operations);
};
